#include <stdio.h>
void Binary(int);
int main(void)
{printf("Enter a number to find binary equivalebt\n");
int num;
scanf("%d", &num);
Binary(num);
    return 0;}

void Binary(int num)
{int temp;
    while(num > 0)
    {
temp = num%2;



    }
}